# OpenAiAssistant

API setup   
    create new nodejs project   
        npm init -y
    install express
        npm install express
    run it
        node app.js

UI setup
    install the packages
        npm install
    install angular cli
        npm install -g @angular/cli
    run it
        ng serve
    